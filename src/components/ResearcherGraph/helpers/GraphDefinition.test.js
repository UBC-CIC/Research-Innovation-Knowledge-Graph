import GraphDefinition from "./GraphDefinition"

test('GraphDefinition returns a correct format',()=> {
    const result = GraphDefinition();
    expect(result).toBeDefined(); //TODO: change this to actually check the format
});